# Copy to .env.wsl.local (gitignored) and set your Windows username.
# This is the folder name under C:\Users\ — often NOT the same as `whoami` in WSL.
#
# Example: if your SDK is C:\Users\8gent\AppData\Local\Android\Sdk
WSL_WINDOWS_USER=8gent
